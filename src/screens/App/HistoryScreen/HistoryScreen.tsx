import {Heading} from '@components/atomic/Heading';
import ViewWrapper from '@components/atomic/ViewWrapper';

export function HistoryScreen() {
  return (
    <ViewWrapper>
      <Heading title="Histórico"/>
    </ViewWrapper>
  );
}
